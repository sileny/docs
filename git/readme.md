# command

```
git init && git config user.name sunsilently && git config user.email sunsilently@outlook.com && git remote add origin https://github.com/sileny/is-array-buffer && git pull https://github.com/sileny/is-array-buffer main && git branch -m master main
```

